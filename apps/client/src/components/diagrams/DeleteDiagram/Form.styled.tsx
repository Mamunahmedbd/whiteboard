import { styled } from 'shared';

// Styled components
export const FormContainer = styled('form', {
  display: 'flex',
  justifyContent: 'flex-end',
  padding: '20px',
  gap: '0.5rem',
  borderRadius: '8px',
  width: '100%',
  margin: '5px auto',
  zIndex: 9999,
});
